package me.newgarbo.garbobase;

/**
 * Main class for the GarboBase, extend this class or just use it as a global
 * API for accessing all the managers and so on. <p> If you do not extend it,
 * then accessing it by simply calling the static instance which is:
 * {@code GarboBase.getInstance()} <p> Or, if you really want to, you can use
 * reflection (you bad boy) and search for private static instance which is:
 * {@code GarboBase.instance}
 * 
 * @author NewGarbo
 * @version 1.0
 */
public class GarboBase
{
	
}
